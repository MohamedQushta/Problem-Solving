#include <bits/stdc++.h>
using namespace std;


int main()
{
 int x = 9, y=10,z;
 z = x > y ? 1 : 20;
int temp = 90, population, base=2;
population = temp > 45 ? base*10 : base*2;
cout << population;

}